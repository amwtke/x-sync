interface:
  display_name: "X-Sync"
  short_description: "在本地页面自由讨论仓库，由 Codex 自动回答并导出 Markdown"
  default_prompt: "Use $x-sync to open or resume a free-form repository chat with automatic Codex replies, optional suggested questions, and Markdown export; honor -d <target-project> when supplied."

policy:
  allow_implicit_invocation: true
